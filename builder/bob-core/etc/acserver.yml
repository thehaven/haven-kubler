api:
  serverName: docker.artifactory.thehavennet.org.uk ## if not provided, use dns of http requests
  port: 443
  https: true
  #username:                # disable basic auth security if not provided
  #password:
STORAGE:
  rootPath: /gentoo   # where to store acis
  unsigned: true            # support unsigned acis
  allowOverride: true       # allow overriding aci that already exists in store
