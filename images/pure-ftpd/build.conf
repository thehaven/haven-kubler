# Used build container, optional, default: value of DEFAULT_BUILDER of namespace thehaven.conf
#BUILDER="thehaven/bob"

# Run build container with `--cap-add SYS_PTRACE`, optional, default: false
#BUILDER_CAPS_SYS_PTRACE=true

# Fully qualified image id (i.e. thehaven/busybox), optional, default: scratch
IMAGE_PARENT="thehaven/openssl"
